# Temporary Deployment Helpers

This directory contains one-off diagnostics, migrations, and experiments.

These scripts are not called by GCP or local-testnet deployment entrypoints.
Promote a helper into the relevant formal directory before making it a required
deployment dependency.
