# Optional machine-specific repository locations.
# The default layout expects these repositories to be siblings of Parth.
PSY_SERVICES_HOME="/path/to/psy-services"
PSY_COMPILER_HOME="/path/to/psy-compiler"
PSY_WALLET_DIR="/path/to/psy-wallet"
PSY_SDK_DIR="/path/to/psy-sdk"
