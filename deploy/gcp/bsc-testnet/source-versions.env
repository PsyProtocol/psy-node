#!/usr/bin/env bash

BSC_COMPAT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=../../bsc-testnet/gcp/source-versions.env
source "$BSC_COMPAT_DIR/../../bsc-testnet/gcp/source-versions.env"
