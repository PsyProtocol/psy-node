# Compatibility Entry Points

The canonical BSC Testnet GCP profile moved to
`deploy/bsc-testnet/gcp/`. These wrappers preserve existing commands while
operators migrate their ignored `config.env` files.
